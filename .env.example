# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=neuralretail
POSTGRES_USER=nr_user
POSTGRES_PASSWORD=CHANGEME

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=CHANGEME

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000
AWS_ACCESS_KEY_ID=CHANGEME
AWS_SECRET_ACCESS_KEY=CHANGEME
MLFLOW_ARTIFACT_BUCKET=nr-mlflow-artifacts

# Feast Feature Store
FEAST_REDIS_HOST=localhost
FEAST_REDIS_PORT=6379
FEAST_S3_BUCKET=nr-feature-store

# JWT Auth
JWT_SECRET_KEY=CHANGEME_min64chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
API_KEY_HASH=CHANGEME_bcrypt_hash

# HashiCorp Vault
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=CHANGEME
VAULT_MOUNT_POINT=neuralretail

# External APIs
OPEN_METEO_BASE_URL=https://api.open-meteo.com/v1
TIMEGPT_API_KEY=CHANGEME

# Airflow
AIRFLOW_FERNET_KEY=CHANGEME
AIRFLOW_SECRET_KEY=CHANGEME

# Slack Alerting
SLACK_WEBHOOK_URL=CHANGEME

# PagerDuty
PAGERDUTY_ROUTING_KEY=CHANGEME

# App
APP_ENV=development
LOG_LEVEL=INFO
ALLOWED_ORIGINS=http://localhost:8501,http://localhost:8000

# Security
INGEST_PII_SALT=CHANGEME_LONG_RANDOM_STRING
